[project]
name = "benchmark-visualizer"
version = "0.1.0"
description = "實驗數據視覺化與比較工具"
requires-python = ">=3.10"
dependencies = [
    "dash>=2.14.0",
    "plotly>=5.18.0",
    "pandas>=2.1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
]
